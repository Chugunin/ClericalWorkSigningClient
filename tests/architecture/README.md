# Architecture tests

The executable boundary checks live in `scripts/check-architecture.mjs` and run through `npm run check:architecture`.
Vitest contract tests will be added here as modules acquire public APIs and explicit dependency contracts.
